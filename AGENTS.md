## UI / UX

Before modifying any frontend, layout, chart, table, CSS, or visual component,
you MUST read and follow `DESIGN.md`.

`DESIGN.md` is the single source of truth for this project's visual design system.

Do not override its desktop layout, LS color system, sizing rules,
or prohibited design patterns unless explicitly requested by the user.
